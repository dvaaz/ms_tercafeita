# Applies to every service in this monorepo as a safety net, in addition to
# each service's own .gitignore.
node_modules/
dist/
build/
.env
.env.development.local
.env.test.local
.env.production.local
.env.local
ENV-BACKUP.md
*.tsbuildinfo
*.log
.DS_Store
Thumbs.db

# Claude Code local session state (not project content)
.claude/
